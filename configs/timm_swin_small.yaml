model:
  import: timm.create_model
  kwargs:
    model_name: swin_small_patch4_window7_224
    pretrained: false
  device: xpu:0

input:
  shape: [8, 3, 224, 224]
  dtype: float32

benchmark:
  warmup: 10
  iters: 50

classes: null

output:
  dir: ./results/swin_small
  latex: true
  csv: true
