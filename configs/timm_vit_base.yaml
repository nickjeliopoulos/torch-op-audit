model:
  import: timm.create_model
  kwargs:
    model_name: vit_base_patch16_224
    pretrained: false
  device: xpu:0

input:
  shape: [8, 3, 224, 224]
  dtype: float32

benchmark:
  warmup: 10
  iters: 50

classes: null

output:
  dir: ./results/vit_base
  latex: true
  csv: true
